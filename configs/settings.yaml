model:
  provider: openai
  embedding_model: text-embedding-3-small
  llm_model: gpt-5-mini

vector_store:
  type: chroma
  path: ./vector_store

data_paths:
  - ./data/notes
  - ./data/code
  - ./data/images
